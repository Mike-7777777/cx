package format

// UI labels used across packages. Centralized here to avoid
// scattered string literals and make future changes easy.
const (
	LabelCtx    = "ctx"
	LabelRate5h = "5h"
	LabelRate7d = "7d"
	LabelStale  = "stale"
	LabelReset  = "reset"
	LabelNoData = "no data"
)
